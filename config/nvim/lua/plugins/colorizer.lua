return {
  "NvChad/nvim-colorizer.lua",
  opts = {
    user_default_options = {
      names = true,        -- поддержка color names (red, blue)
      css = true,          -- включить в CSS
      tailwind = true,     -- поддержка Tailwind классов (по желанию)
      mode = "background", -- можно "foreground" или "virtualtext"
    },
  },
}
